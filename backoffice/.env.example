# Same Supabase project as the app. The secret key reads every client's logs: keep it server-side only.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
# Basic auth for the whole backoffice. Use a long random password (at least 16 characters).
BACKOFFICE_USER=
BACKOFFICE_PASSWORD=
